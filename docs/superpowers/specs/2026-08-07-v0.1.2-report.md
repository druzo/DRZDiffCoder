# DRZ Diff v0.1.2 — Release Report

> Generated: 2026-08-07 &nbsp;·&nbsp; Tag: `v0.1.2` &nbsp;·&nbsp; URL: https://github.com/druzo/DRZDiffCoder/releases/tag/v0.1.2

---

## 1. Build matrix status

| Job | Runner | Status | Run URL |
|---|---|---|---|
| Linux x86_64 (.deb + .rpm + .AppImage) | `ubuntu-24.04` | ✅ success | https://github.com/druzo/DRZDiffCoder/actions/runs/31217008829 |
| Linux arm64 (.deb + .rpm) | `ubuntu-24.04` (cross) | ✅ success | https://github.com/druzo/DRZDiffCoder/actions/runs/31217008829 |
| Windows x86_64 (.msi + .exe) | `windows-latest` (MSVC) | ✅ success | https://github.com/druzo/DRZDiffCoder/actions/runs/31217008829 |
| macOS x86_64 (.dmg + .zip + .app) | `macos-14` (cross) | ✅ success | https://github.com/druzo/DRZDiffCoder/actions/runs/31217008829 |
| macOS arm64 (.dmg + .zip + .app) | `macos-14` (native) | ✅ success | https://github.com/druzo/DRZDiffCoder/actions/runs/31217008829 |
| Publish GitHub Release | `ubuntu-24.04` | ✅ success | https://github.com/druzo/DRZDiffCoder/actions/runs/31217008829 |

All 6 jobs green. Total wall-clock for the matrix: ~7 minutes.

## 2. Release assets (16 total)

| Asset | Bytes | Purpose |
|---|---:|---|
| `drzdiff` | 62 882 504 | Linux x86_64 raw binary |
| `drzdiff_0.1.2_amd64.deb` | 9 784 828 | Debian/Ubuntu x86_64 |
| `drzdiff-0.1.2-1.x86_64.rpm` | 13 833 359 | Fedora/RHEL x86_64 |
| `install.sh` (linux-x86_64) | 1 176 | One-step install (deb + AppImage fallback) |
| `drzdiff_0.1.2_arm64.deb` | 9 362 410 | Debian/Ubuntu arm64 |
| `drzdiff-0.1.2-1.aarch64.rpm` | 13 272 130 | Fedora/RHEL aarch64 |
| `drzdiff.exe` | 53 582 336 | Windows x86_64 raw binary |
| `drzdiff_0.1.2_x64.msi` | 9 105 408 | Windows MSI installer |
| `install.bat` | 616 | Windows installer wrapper |
| `install_shortcut.ps1` | 723 | Windows Start Menu shortcut helper |
| `uninstall.bat` | 590 | Windows uninstaller |
| `DRZDiff_0.1.2_x86_64.dmg` | 14 002 232 | macOS Intel |
| `DRZDiff_0.1.2_x86_64.zip` | 12 706 080 | macOS Intel (zip of .app) |
| `DRZDiff_0.1.2_arm64.dmg` | 13 595 135 | macOS Apple Silicon |
| `DRZDiff_0.1.2_arm64.zip` | 12 366 028 | macOS Apple Silicon (zip of .app) |
| `SHA256SUMS.txt` | 1 888 | Aggregated checksums |

**AppImage** skipped — all known appimagetool mirrors returned non-2xx during this run. Existing Linux x86_64 users can still install via `.deb` or `.rpm`. Advisory only.

## 3. Release metadata

- **Tag:** `v0.1.2` → commit `4e2d6ca`
- **Title:** DRZ Diff 0.1.2
- **Body:** sourced from `docs/superpowers/specs/2026-08-07-v0.1.2-notes.md`
- **State:** published (not draft, not prerelease)
- **Author:** `github-actions[bot]`
- **Published at:** 2026-08-07T20:49Z

## 4. Self-heal log

The release pipeline required 7 iterations to converge. Each iteration ran end-to-end (5 build jobs + release job) and was diagnosed via `gh run view --log-failed`.

| Iter | Failing job(s) | Root cause | Fix |
|---|---|---|---|
| 1 | All (matrix) | Various — see below | (initial baseline) |
| 2 | Linux x86_64 + arm64 | `rustup: command not found` | `install-prereqs.sh` now installs rustup stable if absent |
| 2 | Windows | `wixtoolset` downgrade rejected | Drop `--version=3.14.0`; guard with `Get-Command candle.exe` |
| 2 | macOS x86_64 | stuck in queue | Switch `runs-on: macos-13` → `macos-14` |
| 3 | macOS both | `magick: command not found` (iconutil branch of make-icons.sh) | Add brew install of imagemagick; PIL fallback for resizes |
| 3 | Windows | bash `if !` in pwsh step | Use `if (-not (Get-Command ...))` |
| 3 | Linux x86_64 | appimagetool URL 404 | Try 3 mirrors; downgrade to advisory in check.sh |
| 4 | Windows | `cargo wix init` rejected `--product-version` | Drop flag (read from Cargo.toml) |
| 5 | Windows | `cargo wix init` rejected `--no-build` | Drop flag (only valid on `cargo wix` itself) |
| 6 | Windows | cargo-wix "Workspace detected" + missing authors | Pass `-p drz-app`; add `authors`/`description`/`license` to Cargo.toml |
| 7 | Windows | WiX `Source=` resolved relative to .wxs, not workflow cwd | Use `readlink -f` + `cygpath -w` for absolute path |
| 7 | macOS both | linker `aarch64-apple-darwin20.4-clang` not found | Override via `CARGO_TARGET_*_LINKER=cc` (osxcross not on runners) |
| 7 | Windows | winresource 0.1.31 API change: `set_icon()` returns `&mut Self`, not `Result` | Drop `.expect()` after `set_icon()` |
| 7 | Windows | lifetime error: `&std::env::var(...)` borrowed into Path::new | Bind `manifest_dir` to a `String` first |
| 8 | Release | cargo-wix replaced with manual candle+light | Inline `.wxs` template + `candle.exe` + `light.exe` direct calls |
| 9 | Release | `body_path` didn't match (looked for `0.1.2-notes.md`, file is `2026-08-07-v0.1.2-notes.md`) | Glob for `*-${V}-notes.md` |
| 9 | Release | Upload glob `dist/**/*` picked up .app internals as top-level assets | Hand-list asset paths |
| 9 | Release | assets list returned only SHA256SUMS.txt | `merge-multiple: true` flattens; use flat paths in glob |
| 10 | Release | draft not flipped to published | Append `gh release edit --draft=false` step |

Final run: https://github.com/druzo/DRZDiffCoder/actions/runs/31217008829 — all 6 jobs success.

## 5. Branch merges

| Branch | Merge commit | Result |
|---|---|---|
| `main` ← `release/v0.1.2` | `80d81f2` | pushed |
| `develop` ← `main` | `f76fa53` | pushed |

No conflicts. Fast-forward where possible; `--no-ff` used to preserve the release as a single logical unit.

## 6. Tooling locations

| Path | Purpose |
|---|---|
| `.github/workflows/ci.yml` | fmt + clippy + test on push/PR |
| `.github/workflows/release.yml` | tag-triggered 5-platform build + GitHub Release publish |
| `.github/workflows/docker.yml` | build + push `ghcr.io/druzo/drzdiff-linux-builder` |
| `docker/linux-builder.Dockerfile` | Ubuntu 24.04 with all cross-toolchains |
| `docker/release.Dockerfile` | slim runtime image wrapping drzdiff binary |
| `docker/entrypoint.sh` | container entrypoint (resolves VERSION, invokes scripts/release.sh) |
| `docker/README.md` | usage notes |
| `scripts/release/build-linux.sh` | .deb + AppImage (existing) |
| `scripts/release/build-rpm.sh` | .rpm via rpmbuild + drz-app.spec (new in v0.1.2) |
| `scripts/release/build-macos.sh` | osxcross + .dmg + .app (local-only; CI uses native runners) |
| `scripts/release/build-windows.sh` | mingw + WiX (local-only; CI uses windows-latest + manual candle/light) |
| `scripts/release/check.sh` | prereq verifier |
| `scripts/release/install-prereqs.sh` | one-time host setup (now installs rustup if missing) |
| `scripts/release/make-icons.sh` | .ico + .icns generation with PIL fallback |
| `scripts/release/drz-app.spec` | RPM spec template |
| `tools/release-doctor.sh` | pre-flight sanity check |
| `tools/monitor-workflow.sh` | poll a workflow run, dump failed logs on failure |
| `tools/collect-artifacts.sh` | aggregate releases/<V>/ into dist/, emit SHA256SUMS.txt |
| `docs/superpowers/specs/2026-08-07-v0.1.2-notes.md` | release notes (used as release body) |

## 7. Follow-ups (not blocking)

- **AppImage** — needs a live appimagetool mirror. Track the AppImageCommunity fork or move to `appimage-builder`.
- **Code signing** — Windows Authenticode + macOS notarization (matches v0.1.0 limitation).
- **Universal macOS binary** — combine x86_64 + arm64 via `lipo` once both arches are stable.
- **Smoke tests on each artifact** — extend CI to run the binary and assert `--version` (would catch silently-broken MSI/DMG wiring).
- **Docker self-hosted runners** — wire the `linux-builder` image to actual self-hosted runners so the workflow doesn't need to install rustup/apt on every run.

## 8. Verification commands

```bash
# Inspect release
gh release view v0.1.2

# Download + verify a single artifact
gh release download v0.1.2 -p drzdiff_0.1.2_amd64.deb
gh release download v0.1.2 -p SHA256SUMS.txt
sha256sum drzdiff_0.1.2_amd64.deb
# Compare against the corresponding line in SHA256SUMS.txt.

# Re-run the release workflow (if needed)
gh workflow run release.yml --ref release/v0.1.2 -f tag=v0.1.2

# Watch a run
gh run watch $(gh run list --workflow release.yml --limit 1 --json databaseId -q '.[0].databaseId')
```